version: '3.8'

services:
  sansheng-demo:
    image: cft0808/sansheng-demo:latest
    platform: linux/amd64
    ports:
      - "7891:7891"
    environment:
      - DEMO_MODE=true
    restart: unless-stopped

networks:
  edict-net:
    driver: bridge
